# Pulumi Engine

Core Infrastructure as Code engine managing deployment state, resource graphs, and provider interactions.

**Tech Stack**: Go 1.22, gRPC, OpenTelemetry

**Run Locally**: `go run cmd/pulumi/main.go` (requires `pulumi login` to a backend)