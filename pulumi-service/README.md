# Pulumi Cloud Service

Core SaaS backend for Pulumi Cloud, providing state management, webhooks, deployment orchestration, and REST API.

**Tech Stack**: Java 17, Spring Boot 3.x, PostgreSQL, Redis, AWS S3

**Run Locally**: `./gradlew bootRun` (requires Postgres on localhost:5432)